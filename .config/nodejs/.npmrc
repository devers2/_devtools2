# 1. 글로벌 패키지 설치 경로
prefix=${DEVTOOLS2}/data/.npm-packages

# 2. npm 다운로드 캐시 경로 (성능 최적화)
cache=${DEVTOOLS2}/data/.cache/npm

# 3. 공식 npm 레지스트리 (보안, 무결성 검증, npm audit 지원)
registry=https://registry.npmjs.org/
